<div class="header">
    <span class="logo"><%= link_to 'MyDiary', root_path %></span>
    <% if @user %>
        <div class="header-user">
            <h2 class="user-name"><%= @user.name %></h2>
            <p class="user-logout"><%= link_to 'ログアウト', logout_path, method: :delete %></p>
        </div>
    <% end %>
</div>